<Window x:Class="Ticketing_Harry_Poter.Views.UserDashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mon Espace"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        WindowStyle="None"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- bandeau -->
            <RowDefinition Height="*"/>
            <!-- contenu -->
        </Grid.RowDefinitions>

        <!-- Bandeau : nom utilisateur + actions -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Top">
            <TextBlock Text="Utilisateur : " FontWeight="Bold" FontSize="16"/>
            <TextBlock x:Name="UserNameText" FontSize="16" Margin="0,0,20,0"/>
            <Button Content="Nouveau Ticket"
              Click="NewTicket_Click"
              Margin="0,0,20,0"/>
            <Button Content="Rafraîchir"
              Click="Refresh_Click"/>
        </StackPanel>

        <!-- Contenu : liste + détails/commentaires -->
        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <!-- DataGrid -->
                <RowDefinition Height="1.2*"/>
                <!-- zone commentaires -->
            </Grid.RowDefinitions>

            <!-- DataGrid des tickets de l'utilisateur -->
            <DataGrid x:Name="MyTicketsGrid"
                Grid.Row="0"
                AutoGenerateColumns="False"
                IsReadOnly="True"
                SelectionChanged="MyTicketsGrid_SelectionChanged"
                Margin="0,0,0,10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"     Binding="{Binding Id}"            Width="40"/>
                    <DataGridTextColumn Header="Titre"  Binding="{Binding Title}"         Width="*"/>
                    <DataGridTextColumn Header="Statut" Binding="{Binding Status}"        Width="100"/>
                    <DataGridTextColumn Header="Niv."   Binding="{Binding IncidentLevel}" Width="60"/>
                    <DataGridTextColumn Header="Créé le" Binding="{Binding CreatedAt}"     Width="150"/>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Commentaires & nouveau commentaire -->
            <StackPanel Grid.Row="1" Orientation="Vertical">
                <TextBlock Text="Commentaires" FontWeight="Bold" Margin="0,0,0,5"/>
                <ListBox x:Name="CommentsListUser"
                 Height="100"
                 DisplayMemberPath="Content"
                 Margin="0,0,0,5"/>
                <TextBox x:Name="NewCommentBoxUser"
                 Height="60"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 Margin="0,0,0,5"/>
                <Button Content="Ajouter Commentaire"
                HorizontalAlignment="Right"
                Click="AddComment_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
